---
phase: 5-pdf-foundation
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/app/formview/page.tsx
autonomous: true

must_haves:
  truths:
    - "PDF viewer displays in left panel with zoom/page controls"
    - "AI Assistant panel on right matches existing design"
    - "Dark theme consistent with forms/[id]/page.tsx"
    - "Mobile responsive - stacks vertically"
    - "PDF source from user selection or upload"
  artifacts:
    - path: "frontend/src/app/formview/page.tsx"
      provides: "Dual-panel form view with PDF on left, AI chat on right"
---

<objective>
Update /formview page to display actual PDF on left instead of structured form UI, keeping AI assistant on right.

Design:
- Left: PDF viewer (react-pdf) with zoom and page navigation
- Right: AI Assistant chat panel
- Dark theme matching existing codebase
- Responsive: stack on mobile
</objective>

<tasks>

<task type="auto">
  <name>Replace structured form UI with PDF viewer</name>
  <files>frontend/src/app/formview/page.tsx</files>
  <action>
Replace the current ContentItemCard-based left panel with PDFViewer component:

1. Import PDFViewer from '@/components/PDFViewer'
2. Import usePDFStore from '@/store/pdfStore'
3. Replace sampleFormData items with PDFViewer component
4. Reuse AI assistant panel design from current implementation
5. Dark theme: bg-gray-900, border-gray-800
6. Responsive: flex-col on mobile, flex-row on desktop
  </action>
  <verify>
    - Page loads without errors
    - PDF displays with controls
    - AI panel remains functional
  </verify>
</task>

</tasks>
